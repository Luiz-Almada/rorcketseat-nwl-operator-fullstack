// Configuração do PostCSS para processar estilos CSS
// Inclui o plugin do Tailwind CSS para geração de utilitários

const config = {
  plugins: {
    "@tailwindcss/postcss": {}
  }
}

export default config
